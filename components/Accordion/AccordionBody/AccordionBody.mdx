# tds-accordion-body

## Properties

| Property | Attribute | Type      | Default |
|----------|-----------|-----------|---------|
| `open`   | `open`    | `boolean` | false   |

## Events

| Event          | Type                              |
|----------------|-----------------------------------|
| `body-toggled` | `CustomEvent<{ open: boolean; }>` |
